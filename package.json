{
  "name": "portal-fundacji",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "prisma generate && next build",
    "check": "prisma validate && node --test tests/*.test.mjs",
    "admin:set-role": "node scripts/ustaw-role.mjs",
    "start": "node server.js",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@auth/prisma-adapter": "^2.11.3",
    "@prisma/client": "^6.1.0",
    "next": "16.3.5",
    "next-auth": "^5.0.0-beta.25",
    "nodemailer": "10.0.9",
    "react": "19.3.0",
    "react-dom": "19.3.0"
  },
  "devDependencies": {
    "prisma": "^6.1.0"
  },
  "overrides": {
    "deepmerge-ts": "8.0.0",
    "nodemailer": "10.0.9"
  }
}
